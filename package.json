{
  "name": "ofri-kraus-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "bundle:upload": "esbuild scripts/admin-upload-entry.js --bundle --format=esm --platform=browser --minify --outfile=admin-upload.bundle.js",
    "security": "node scripts/security_checks.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
